# X thread

Do not post this without the demo video. The whole thing is a visual joke; as
text it reads like a productivity app, which is the one thing it is not.

**1/** (attach the ~6s loop: robot fades in over a real editor, types a line,
disappears)

```
i made a tiny robot that pops up in the corner of my screen and insults my work
ethic

it types one line, then leaves

free, open source, mac + windows
```

**2/** (attach a screenshot of the settings window)

```
every message is a line in a text file

five packs ship. the mean one is called obituary. you can point it at your own
file and it reads that instead
```

**3/**

```
it draws over whatever you're doing and clicks pass straight through it, so it
can't take focus or eat a keystroke

no dock icon, no taskbar entry, no backend, no account, no telemetry
```

**4/**

```
MIT, free, and the packs are the part i'd most like people to add to

https://grindbot.melbora.com
```

**Notes**

- Post 1 has to stand alone. If someone sees only the video and the first line,
  it should still land.
- No emoji, no "excited to share", no thread-starting rocket.
- Reply to your own thread with the repo link if engagement is decent rather
  than putting the GitHub URL in post 1.
